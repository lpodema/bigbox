setup:
  addons:
    plan: heroku-postgresql
    as: db_bigbox

build:
  docker:
    backend: Dockerfile

release:
  command: python manage.py migrate
  image: backend
run:
  backend: 
    command: python manage.py runserver